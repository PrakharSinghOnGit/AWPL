import React from "react";

const Terminal = () => {
  return (
    <div
      className="section"
      style={{
        borderRadius: "0 10px 10px 0",
      }}
    ></div>
  );
};

export default Terminal;
