import React from "react";

const Terminal = () => {
  return (
    <div
      className="section"
      style={{
        borderRadius: "0 10px 10px 0",
        backgroundColor: "white",
      }}
    ></div>
  );
};

export default Terminal;
